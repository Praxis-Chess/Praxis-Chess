# Application secrets — copy from application.example.yml and fill in
backend/src/main/resources/application.yml

# Claude Code project instructions (local only)
CLAUDE.md

# Maven
backend/target/
*.class
*.jar

# Docker infra data
backend/infra/dev-postgresql/postgres_data/

# IDE
.idea/workspace.xml
*.iml
.vscode/

# Frontend
frontend/node_modules/
frontend/node/
frontend/dist/

# OS
.DS_Store
Thumbs.db
"application.yml" 
"tts-service/models/*.onnx" 

# Playwright
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/
frontend/playwright/.cache/

# infra scripts: background service logs
.infra/

# Local design docs and plans — kept on disk, never committed
technical-docs/

# Local secrets — never committed
.env
# Stale copy of the Spring config. IntelliJ runs from backend/, so Spring never
# loaded it; the live config is backend/src/main/resources/application.yml.
/application.yml
# Holds this SearXNG instance's secret_key. infra-up.sh generates it from
# searxng/settings.example.yml with a fresh random key.
searxng/settings.yml
